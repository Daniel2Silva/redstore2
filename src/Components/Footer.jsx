import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
  return (
    <section className={`${styles.footerC}`}>
      <div className={`${styles.footer} container `}>
        <h3>dn2fe@icloud.com</h3>
      </div>
    </section>
  );
};

export default Footer;
